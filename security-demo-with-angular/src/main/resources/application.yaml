spring:
  datasource:
    url: jdbc:mariadb://127.0.0.1:3306/security-jwt-demo-with-angular?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC&autoReconnect=true
    username: root
    password: test1234
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: on
server:
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
